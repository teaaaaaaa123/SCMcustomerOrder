# 客商下单技能配置示例
# 本文件仅用于说明配置，不要填写真实密钥，也不要提交 config.yaml。

# API认证信息
access_key_id: "${ACCESS_KEY_ID}"
access_key_secret: "${ACCESS_KEY_SECRET}"

# 订单存储配置
storage:
  type: json
  path: "orders.json"

# 日志配置
logging:
  level: INFO
  file: "skill.log"
